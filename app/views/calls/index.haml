%h1= 'Feed of the 10 last calls emitted or received'
-# filters, auto reloading and some styles could be nice here :)
- if @calls.empty?
  %p= 'No calls to display yet, try to make one to +33186766957'
- else
  %ul
    - @calls.each do |call|
      %li
        = render partial: call